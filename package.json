{
  "name": "project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tailwindcss -i ./input.css -o ./src/css/output.css",
    "watch": "npx tailwindcss -i ./input.css -o ./src/css/output.css --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tw-elements": "^1.0.0-alpha12"
  },
  "devDependencies": {
    "netlify-cli": "^10.12.1"
  }
}
